# 项目问题台账

本文档用于记录当前仓库的结构问题、疑似多余文件、未接线模块、文档偏差以及配置风险。

**注意：** 本文档的目的是先把问题记录清楚，便于后续分步骤治理；被列入本文档的文件或目录不代表应该立刻删除。

## 1. 当前真实主结构

当前仓库的真实主路径主要是：

- `app.py`
  - Flask 主入口
  - 页面路由
  - Socket.IO 事件处理
  - 教学、历史、健康检查、TTS 等主要 API
- `core/`
  - `detector.py`：视觉识别核心
  - `filters.py`：滤波逻辑
  - `user_stats.py`：训练统计聚合
  - `llm_service.py`：教学建议/和弦推荐的大模型封装
  - `tts_service.py`：TTS 合成与缓存
- `api/auth.py`
  - 登录、注册、退出、当前用户状态
- `api/chords.py`
  - 和弦静态数据与查询接口
- `templates/`
  - 页面模板
- `static/`
  - 页面 CSS / JS / 图片资源
- `models.py`
  - SQLAlchemy 数据模型
- `config.py`
  - 模型路径、数据库连接、LLM/TTS 配置和识别参数

## 2. 项目结构问题

### 2.1 API 组织不统一

当前 API 逻辑同时分散在 `app.py` 和 `api/` 目录中：

- `app.py` 中直接实现了大量 `/api/...` 路由
- `api/auth.py`、`api/chords.py` 是已接入主应用的蓝图
- `api/solo.py`、`api/teach.py` 也定义了蓝图，但实际主入口并未统一注册它们

这会带来几个问题：

- 协作者难以快速判断“真实生效代码”在哪
- 同类功能可能存在重复实现
- 后续重构 API 时容易误改未接线模块

### 2.2 `api_bp` 已定义但未接线

`api/__init__.py` 中定义了 `api_bp`，并注册了：

- `solo_bp`
- `teach_bp`
- `chords_bp`

但当前 `app.py` 并没有注册 `api_bp`。

这说明仓库里存在一套“计划中的统一 API 蓝图组织方式”，但没有真正接入运行主路径。

### 2.3 README 与真实结构不一致

当前旧版 `README.md` 仍然以早期目录树为主，存在明显偏差：

- 提到了当前仓库中并不存在或未确认存在的结构，例如 `try/`
- 没有准确描述当前真实主路径
- 没有介绍教学驾驶舱、历史记录、登录、TTS、LLM 等真实模块
- 没有说明模块级验证方式
- 没有协作者使用说明和 Git 协作建议

## 3. 疑似多余文件/目录与运行残留

以下文件或目录不像源码主资产，更像备份、运行产物或临时记录：

### 3.1 明显运行残留

- `nohup.out`
  - 标准运行日志输出文件
- `tts_cache/`
  - TTS 运行缓存目录，内部为生成的音频缓存

### 3.2 明显备份/人工副本

- `app.py.bak`
- `app.py.eventlet_backup`

这两个文件命名方式都明显表明它们是历史备份，不应长期与主代码并列保留在根目录。

### 3.3 命名异常、用途不清晰的文件

- `upervisor 是否在管理`

这个文件名不像源码、配置文件或正式文档，更像人工排查记录。应确认：

- 是否还有保留价值
- 是否应该迁移到正式文档目录
- 是否应该从仓库中删除

## 4. 未接线 / 疑似废弃模块

### 4.1 `api/__init__.py`

- 定义了统一的 `api_bp`
- 当前未被 `app.py` 注册
- 属于“已设计但未启用”的结构

### 4.2 `api/solo.py`

当前判断为疑似旧实现或未接线模块，原因包括：

- Blueprint 未通过 `api_bp` 接入主应用
- 与 `app.py` 中实际使用的部分逻辑存在功能重叠
- 作为真实运行路径的证据不足

### 4.3 `api/teach.py`

当前判断为疑似旧实现或未接线模块，原因包括：

- Blueprint 未接入主应用
- 与 `app.py` 中已经实现的教学驾驶舱 API 并存
- 部分逻辑更像演示或早期版本

## 5. 文档问题

当前仓库文档存在以下问题：

- README 过时
- 缺少“面向协作者”的结构说明
- 缺少运行前准备说明
- 缺少“除 `python app.py` 外”模块验证方式说明
- 缺少多人协作 Git 工作方式说明
- 缺少对未接线模块与真实主路径的提醒

## 6. 配置与安全问题

### 6.1 `.gitignore` 原先不完整

原始 `.gitignore` 偏向通用 Python 模板，未覆盖当前项目特有的本地噪音：

- `nohup.out`
- `tts_cache/`
- 本地备份文件
- Claude 相关配置目录

### 6.2 `config.py` 中存在高风险硬编码配置

当前 `config.py` 中存在一些应视为高风险的配置事实，例如：

- 硬编码的 Flask 密钥
- 硬编码的大模型 API Key
- 硬编码的数据库连接字符串

这些配置不适合作为长期协作方式，应逐步迁移到环境变量或本地配置文件方案。

### 6.3 模型文件版本控制策略未明确

当前仓库中包含：

- `models/best.pt`
- `models/hand_landmarker.task`

这两个文件目前属于版本库中的实际资产，但团队尚未明确：

- 是否继续直接放在 Git 仓库
- 是否改用外部下载方式
- 是否引入 Git LFS 或发布包管理策略

在策略明确之前，不建议贸然把模型文件加入 `.gitignore`。

## 7. 运行与验证方面的认知问题

### 7.1 除 `python app.py` 外，独立入口很少

当前仓库中，除完整入口 `python app.py` 之外，最明确的独立验证脚本是：

- `python benchmark.py`

它用于验证视觉识别/指板检测链路，但仍依赖：

- 模型文件
- 图像输入（默认 `test_guitar.jpg`）
- OpenCV / YOLO 运行环境

### 7.2 页面的可独立预览程度不同

- `tuning` 页面最接近可轻量预览的页面
- `solo`、`teach`、`history` 都明显依赖后端接口、数据库或 Socket.IO，不适合作为纯静态页面进行完整功能验收

## 8. 后续建议优先级

### P0

- 重写 `README.md`
- 补全 `.gitignore`
- 建立本问题台账 `project_issue.md`

### P1

- 确认并清理已跟踪的运行残留、备份文件和缓存
- 明确模型文件版本控制策略
- 规划 `config.py` 中敏感配置的治理方案

### P2

- 梳理 `app.py` 与 `api/` 的边界
- 明确 `api/solo.py`、`api/teach.py`、`api/__init__.py` 的保留/接线/移除策略
- 在后续重构中统一 API 组织方式

## 9. 关于 `.gitignore` 的补充说明

`.gitignore` 只会影响**未被 Git 跟踪**的文件。

对于历史上已经提交过的缓存、日志、备份文件，即使后来加入了忽略规则，也仍然需要通过 Git 索引移除（例如 `git rm --cached`）后，才能真正停止跟踪。
